# AutoResearch 搜索策略优化方案

## 背景

当前系统已经具备并行候选生成、实验调度、registry 记录、novelty judge 和失败方向记录。但候选生成仍主要依赖 agent 自行决定“优化哪里”和“怎么优化”，runner 只提供历史 guidance 和重复性约束。

这会导致搜索缺少全局观：agent 能参考历史，但没有显式的搜索状态、父节点选择、搜索算子、方向配额和探索/利用权衡。

参考论文：

1. **Bilevel Autoresearch: Meta-Autoresearching Itself**
2. **AI Research Agents for Machine Learning: Search, Exploration, and Generalization in MLE-bench**
3. **The AI Scientist-v2: Workshop-Level Automated Scientific Discovery via Agentic Tree Search**

共同启发是：不要只让 LLM 自由提出实验，而应由外层搜索策略决定候选扩展方向，agent 负责执行具体代码修改。

## 最需要改：增加最小搜索策略层

在 `autorunner/runner.py` 的候选生成循环前增加一个很薄的 search policy。runner 显式选择：

- 基于哪个父节点继续
- 使用哪个搜索算子
- 目标优化方向是什么
- 哪些方向需要避开
- 当前候选属于探索还是利用

agent 不再自由选择研究方向，而是执行 runner 给出的 `SEARCH_PLAN`。

### 新增模块

新增文件：

```text
autorunner/search_policy.py
```

### 新增数据结构

定义 `SearchPlan`：

```python
class SearchPlan:
    plan_id: str
    # 本次搜索计划的唯一 ID，用于在 registry、日志和候选 meta 中追踪来源。

    parent_candidate_id: str | None
    # 要扩展的父候选 ID；如果从 baseline/root 开始扩展，则为 None。

    parent_ref: str
    # 父节点对应的代码引用，通常是 best_train.py、某个 candidate 的 train.py，或 baseline 标识。

    operator: str
    # 搜索算子名称，表示这次要怎么扩展，例如 explore_new_mechanism、exploit_best_mechanism、pivot_near_miss。

    target_direction_key: str
    # 本次计划指定的优化方向，例如 architecture.attention、optimizer.lr_schedule、regularization.norm。

    intent: str
    # 给实现 agent 的简短任务意图，说明这次实验要验证什么假设。
    # 第一版可以由 plan agent 在 runner 约束下生成，而不是由实现 agent 自由生成。

    rationale: str
    # 给实现 agent 阅读的任务理由，解释为什么本轮选择这个方向和算子。
    # 内容可以由 plan agent 基于 runner 提供的统计事实生成，但不能改写 runner 选定的边界条件。

    avoid_direction_keys: list[str]
    # 本次生成时需要避开的方向，通常来自 active、recent failure、blocked duplicate 或连续失败 family。

    priority: float
    # 搜索策略给该计划的优先级分数，用于排序、调度或在资源不足时决定先跑哪个候选。
```

### 最小搜索算子

先实现四类固定算子：

- `explore_new_mechanism`：探索未充分尝试的新机制。
- `exploit_best_mechanism`：沿当前最佳方向做局部改进。
- `pivot_near_miss`：对接近成功但未提升的方向做关键机制替换。
- `avoid_failed_family`：避开连续失败或重复阻塞的方向，强制转向。

### 策略选择

从 `experiment_registry.jsonl` 统计：

- 每个 `direction_key` 的尝试次数
- keep 次数和 keep 率
- 最好 `val_bpb`
- 最近失败原因
- active / queued / running 占用
- duplicate block 次数

第一版策略建议用 epsilon-greedy 或简化 UCB：

- 未充分尝试的算子优先获得基础探索机会。
- 历史收益高的方向获得更高分数。
- active、queued、running 中已有的方向加 penalty。
- 连续失败、重复阻塞、repair out of scope 的方向加 penalty。
- 保留少量随机探索，避免长期锁死在局部最优。

### 折中版：算法定边界，plan agent 填语义

`intent` 和 `rationale` 这类字段需要一定语义判断。更合理的折中方案是：

```text
script / search_policy:
  - 统计每个方向的成功数、失败数、keep 率、best reward
  - 决定当前 search_mode
  - 决定 parent
  - 决定 operator
  - 决定 target_direction_key
  - 决定 avoid_direction_keys
  - 决定预算、priority 和是否切换算子

plan agent:
  - 在上述硬约束内写 intent
  - 基于统计事实写 rationale
  - 写 experiment brief
  - 写 expected mechanism
  - 写 success / failure interpretation

runner validator:
  - 检查 plan agent 没有改 operator
  - 检查 plan agent 没有改 target_direction_key
  - 检查没有触碰 forbidden / avoid directions
  - 检查 mechanism_search 不是纯调参
  - 检查输出是合法 JSON
```

也就是说：

```text
算法负责边界和预算，plan agent 负责把边界翻译成高质量研究任务。
```

这样比纯模板更灵活，也比让实现 agent 自由选方向更可控。

### plan agent 输入输出

plan agent 的输入应是 runner 生成的硬约束和统计摘要：

```json
{
  "search_mode": "mechanism_search",
  "operator": "pivot_near_miss",
  "target_direction_key": "architecture.attention",
  "parent_candidate_id": "cand-000123",
  "avoid_direction_keys": ["architecture.width_depth", "optimizer.lr_schedule"],
  "stats": {
    "target_visits": 4,
    "target_keep_count": 1,
    "recent_failure_reasons": ["metric_not_improved"],
    "active_directions": ["regularization.dropout"]
  }
}
```

plan agent 的输出必须是 JSON，并且只能补充语义字段：

```json
{
  "intent": "Replace the concrete attention modification with a mechanism-level alternative that changes how context is mixed, without tuning width, depth, learning rate, or dropout.",
  "rationale": "The attention family has one prior near-miss and low current active coverage, while recent width/depth and optimizer directions are saturated or blocked.",
  "experiment_brief": "Implement one minimal attention-path mechanism change and keep unrelated training settings fixed.",
  "expected_mechanism": "The change should improve token mixing or gradient flow through the attention path rather than increasing capacity.",
  "success_interpretation": "A lower val_bpb suggests the mechanism improves useful context mixing.",
  "failure_interpretation": "No improvement suggests this attention mechanism is not useful under the current baseline, unless training instability is observed."
}
```

runner 应把这些字段合并回 `SearchPlan`，但保留原始硬约束字段不被覆盖。

### 和参考论文的对应关系

这个折中版对应三篇论文中的常见分工：

- AI Scientist-v2：系统用 progressive tree search 和 experiment manager 控制搜索过程，LLM 负责 idea、实验计划、实现和结果分析等语义工作。
- MLE-bench AI Research Agents：solver 被拆成 search policy 和 operators。search policy 负责探索策略，LLM operator 负责生成具体解或修改。
- Bilevel Autoresearch：外层 loop 优化内层搜索机制本身。这个方向可以后续作为 meta-search 引入，而不是第一版每个候选都让 agent 自由改策略。

因此，本系统第一版不让 plan agent 决定预算、父节点和算子，只让它把已选边界翻译成更清晰、更有研究价值的任务说明。

### runner 接入点

在 `runner.py` 的 `_generation_loop` 中，生成候选 ID 后先调用 search policy：

```python
plan = select_search_plan(
    registry_items=_load_experiment_registry(),
    active_candidate_ids=...,
    best_train_file=BEST_TRAIN_FILE,
)
plan = complete_search_plan_with_plan_agent(plan)
plan = validate_search_plan(plan)
```

然后 `_create_candidate(...)` 增加参数：

```python
search_plan: SearchPlan
```

并把提示词从“你需要产出一个新候选方向”改成：

```text
你不能自由选择研究方向。
你必须执行 runner 给出的 SEARCH_PLAN。
你只负责把该计划落实到 train.py。
如果认为计划不可行，只能做最接近该计划的最小可运行实现，并在 DESCRIPTION 中说明偏差。
```

`SEARCH_PLAN` 应包含在 `extra_hints` 中。

### 预期收益

这个改动最小，但会立即改变系统性质：

- runner 负责全局搜索。
- agent 负责局部实现。
- novelty judge 负责阻止重复。
- registry 负责沉淀搜索状态。

系统不再是多个 agent 各自凭感觉提出实验，而是沿显式策略进行受控探索。

## 次要需要改：把 registry 变成搜索树

当前 registry 更像日志。下一步应把它扩展成轻量搜索树。

### 新增字段

在候选 proposal 和最终结果中增加：

- `search_parent_id`
- `search_operator`
- `search_depth`
- `search_plan_id`
- `search_score`
- `reward`
- `parent_val_bpb`

### reward 定义

第一版 reward 可以保持简单：

```text
completed + improved: 正 reward，使用相对 val_bpb 改善幅度
completed + not improved: 小负 reward
blocked_duplicate: 负 reward
generation_failed / compile_failed: 中等负 reward
runtime failure / repair_out_of_scope: 强负 reward
```

### 父节点选择

search policy 可以基于 reward 和 visit count 选择父节点：

- 优先扩展高 reward 的 keep 节点。
- 对 near miss 的 discard 节点允许 pivot。
- 对重复阻塞或连续失败分支降低优先级。
- 对已有 active 子节点的父节点降低优先级。

这一步能让系统从“参考历史记录”升级为“在实验树上扩展节点”。

## 同步机制：Epoch 搜索和软 Barrier

可以把整个搜索过程想象成一棵树：

```text
baseline(root)
  -> epoch 1: 多个第一层方向并行探索
  -> summarize epoch 1
  -> epoch 2: 基于汇总结果扩展下一层
  -> summarize epoch 2
  -> ...
```

这里需要同步机制，但不建议做“所有 agent 必须等最后一个完成”的硬同步。更合适的是：

```text
分层 wavefront 搜索 + 软 barrier + 汇总节点
```

同步用于控制决策节奏，而不是让 GPU 长时间等待最慢实验。

### 为什么不做纯硬同步

硬同步存在几个问题：

- 最慢实验会拖住所有 GPU。
- timeout、repair、多次失败的候选会拉低整体吞吐。
- 单个实验结果存在噪声，等所有实验完成不一定显著提高决策质量。
- 多个 agent 的有效改动不能直接合并成新 best，因为改动之间可能互相冲突。

因此，第一版应采用软 barrier。

### Epoch 定义

每个 epoch 是一次受控并行扩展：

```text
epoch_id: 3
parent_node: 当前 best 或某个高潜力节点
budget: 8 个候选
directions:
  - optimizer.lr_schedule
  - architecture.attention
  - regularization.dropout
  - data.curriculum
  - kernel.performance
  - training.stability
```

runner 在 epoch 开始时为每个候选生成不同的 `SearchPlan`。agent 不能自由选择方向，只能执行分配的计划。

### 新增数据结构

定义 `SearchEpoch`：

```python
@dataclass
class SearchEpoch:
    epoch_id: int
    parent_candidate_id: str | None
    parent_ref: str
    status: str
    planned_candidates: int
    min_completed: int
    max_epoch_sec: int
    started_at: float
```

候选 registry 增加字段：

```text
epoch_id
epoch_status
search_depth
search_parent_id
search_operator
assigned_direction
reward
```

### 软 Barrier 结束条件

一个 epoch 满足任一条件即可进入 summarize：

```text
1. 本轮所有候选完成
2. 已完成数量 >= min_completed，例如 6/8
3. epoch 运行时间超过 max_epoch_sec
4. 已经出现明显 winner，且剩余候选长时间未完成
```

未完成候选可以先标记为 `late`，继续跑完后进入下一轮 summary；第一版不需要实现取消任务。

### Epoch Summary

每轮结束后生成：

```text
autorunner/artifacts/epoch_summary_{epoch_id}.json
```

内容包括：

- 本轮 best candidate
- 每个 direction 的结果
- 哪些方向值得继续 exploit
- 哪些方向应该 pivot
- 哪些方向应该冻结或避开
- 下一轮建议的 operator 配比

summary 只负责指导下一层搜索，不直接改变 best。

### 不要直接合并代码

需要特别注意：多个有效候选不能直接合并成一个新版本。

不要这样做：

```text
A 有效 + B 有效 => 直接生成 AB 作为新 best
```

应该这样做：

```text
A 有效 + B 有效 => 创建 merge(A, B) 候选 => 跑实验 => keep/discard
```

也就是说，“汇总版本”必须作为普通候选重新训练验证。只有验证提升后，才能成为新的 best。

### Epoch Manager 接入方式

在 `runner.py` 中增加 epoch manager：

```text
start_epoch()
  -> select parent
  -> select K directions
  -> create K SearchPlans

generation_loop()
  -> 只生成当前 epoch 的 plans
  -> 不再无限 low_watermark 自由补队列

completion_loop()
  -> 收集结果
  -> 判断 epoch 是否达到 soft barrier

summarize_epoch()
  -> 统计 winner / failed families / promising pivots
  -> 写 epoch_summary.json
  -> 生成下一轮 frontier
```

第一版不需要完全重写并行调度，只需要让 generation loop 从“自由补队列”改成“消费当前 epoch 的 plan 队列”。

### 推荐 operator 配比

每个 epoch 可以先使用固定比例：

```text
40% explore_new_mechanism
30% exploit_best_mechanism
20% pivot_near_miss
10% merge_or_ablate
```

后续再根据 epoch summary 自动调整。

## SearchMode 设计：把调参作为动态搜索动作

不建议照搬 The AI Scientist-v2 的刚性四阶段。那篇论文的起点通常是从 research idea 生成 demo，再做调参、研究执行和论文式消融；本系统的常见起点是用户已经提供 baseline，runner 负责持续优化算法代码。

因此，调参、机制改动和验证不应被固定成线性阶段，而应作为每个 epoch 或每个 `SearchPlan` 的动态 `search_mode`。

### 不采用刚性阶段

不要把流程固定成：

```text
先调参 20 次
再机制创新 80 次
再验证 20 次
```

更合理的是：

```text
发现 baseline 不稳 -> control_tune
baseline 稳定 -> mechanism_search
机制候选失败但像训练问题 -> local_tune_after_mechanism
机制候选有效 -> verify_winner / simplify_winner
两个方向分别有效 -> merge_validate
验证失败 -> pivot_after_failure 或回到 mechanism_search
```

也就是说，`search_mode` 是搜索策略的状态和约束开关，不是一次性走完的流水线。

### 推荐 SearchMode

```text
baseline_intake:
  读取用户已有 baseline，确认可运行、指标可解析、best 初始化。
  这通常不计入正式实验 budget。

control_tune:
  只允许调整训练稳定性和关键超参。
  目标是让当前节点可比较，而不是追求长期调参收益。

mechanism_search:
  默认主模式。必须改变机制。
  例如 attention pattern、loss term、optimizer update、value path、routing、curriculum、normalization、regularization 结构。

local_tune_after_mechanism:
  只绑定到某个机制改动之后。
  用于判断该机制是否因为局部超参不合适而被误杀。

verify_winner:
  只在出现明显 winner 后触发。
  用 rerun 或短复现确认提升不是偶然噪声。

merge_validate:
  当两个方向分别有效时，创建 merge candidate 并重新训练验证。
  不允许直接把两个成功改动合并成新 best。

simplify_winner:
  对复杂但有效的 winner 做简化，确认收益来自核心机制而不是无关复杂度。

pivot_after_failure:
  对 near-miss 或重复失败方向保留大目标，但替换具体机制。
```

### 动态切换规则

第一版可以用规则触发：

```text
如果 baseline 从未成功运行:
  -> baseline_intake

如果最近多个候选失败原因是 training_instability 或 compile/runtime 问题:
  -> control_tune

如果某个 mechanism candidate 接近提升但未超过阈值:
  -> local_tune_after_mechanism 或 pivot_after_failure

如果当前 best 刚出现，且提升明显:
  -> verify_winner

如果两个不同 direction_key 都有 verified winner:
  -> merge_validate

如果 winner 很复杂但收益小:
  -> simplify_winner

默认:
  -> mechanism_search
```

### 人类研究员的实际节奏

人类研究员通常不是严格线性的：

```text
建立可信 baseline
-> 粗调关键超参
-> 提出结构性假设
-> 做最小机制改动
-> 为该机制改动做局部调参
-> 比较、消融、回滚或继续扩展
```

也就是说，调参和方法创新是穿插进行的，但层级不同：

- 调参用于让训练稳定、比较公平。
- 方法改动用于测试新的机制假设。
- 每个重要方法改动后，通常需要局部重新调参，否则可能误杀好想法。
- 如果系统长期只改 learning rate、batch size、dropout、width/depth、warmup、训练步数等数字，它仍停留在 engineering tuning，不是系统架构创新。

### 对 runner 的硬约束

需要显式区分不同 `search_mode` 下允许的 operator：

```text
control_tune:
  只允许调整训练稳定性和关键超参。
  目标是让当前节点可比较。

mechanism_search:
  必须改变机制。
  例如 attention pattern、loss term、optimizer update、value path、routing、curriculum、normalization、regularization 结构。
```

`mechanism_search` 候选应增加硬约束：

```text
如果候选只修改数字常量、训练步数、学习率、batch size、dropout、hidden size、head 数或 depth，
且没有引入新的机制或改变已有机制的计算路径，
则标记为 low_novelty 或 out_of_scope，不进入正式实验预算。
```

### 推荐预算先验

预算不应刚性分阶段，但可以给不同模式设 prior：

```text
baseline_intake:
  不计入正式实验 budget，启动或 baseline 变化时触发。

control_tune:
  10% 到 15% 上限，只在不稳定或新机制需要局部公平比较时触发。

mechanism_search:
  70% 到 85%，默认主预算。

verify_winner / merge_validate / simplify_winner:
  0% 到 15%，只在出现 winner 后触发。
```

这些比例是 prior，不是固定流程。runner 可以根据失败原因、winner 出现、near-miss 信号动态切换。

### 和 epoch 搜索的关系

epoch 是并行搜索的执行节奏，`search_mode` 是当前 plan 的角色和约束。

```text
epoch 控制：这一轮并行跑哪些 plan，何时 summarize。
search_mode 控制：这些 plan 为什么存在、允许改什么、不允许改什么。
```

例如：

- `control_tune` mode 的 epoch 只生成稳定性和关键超参计划。
- `mechanism_search` mode 的 epoch 主要生成机制级改动，并禁止纯调参。
- `verify_winner` mode 的 epoch 生成 rerun / replicate plans。
- `merge_validate` mode 的 epoch 生成 merge candidate，并要求重新验证。

### 对 novelty judge 的补充要求

novelty judge 不只判断“是否和历史重复”，还应判断“是否只是调参”。

返回 payload 可以增加：

```json
{
  "allow_run": true,
  "duplicate_level": "distinct",
  "innovation_type": "mechanism_search",
  "is_pure_tuning": false,
  "mechanism_changed": true,
  "reason": "...",
  "required_pivot": "..."
}
```

对于 `mechanism_search`：

```text
allow_run = false
if is_pure_tuning == true or mechanism_changed == false
```

这样可以把调参限制在支撑 baseline 和局部公平比较的角色上，把主要预算留给机制级创新。

## 最后补充：调度增强和 meta-search

在前两步稳定后，再考虑补充更复杂的机制。

### Artifact 目录结构

`autorunner/artifacts` 不直接堆所有文件，按用途分目录：

```text
autorunner/artifacts/
  state/
    experiment_registry.jsonl
    parallel_queue.jsonl
    failure_directions.json
  guidance/
    generator_guidance.md
  snapshots/
    best_train.py
  alerts/
    fatal_stop.json
  tree/
    search_tree.json
    search_tree.html
    search_tree_demo.json
    search_tree_demo.html
```

候选级文件继续放在：

```text
autorunner/candidates/cand-xxxxxx/
  train.py
  meta.json
  run.log
  run_repair_*.log
  llm_*.log
```

### 搜索树 Artifact

先实现手动导出的搜索树 artifact，不接入实时 dashboard：

```bash
python autorunner/search_tree_export.py
```

默认输出：

```text
autorunner/artifacts/tree/search_tree.json
```

该 artifact 从以下文件汇总搜索历史：

```text
autorunner/artifacts/state/experiment_registry.jsonl
results.tsv
autorunner/artifacts/state/parallel_queue.jsonl
```

输出结构包含：

- `nodes`：搜索节点，包括 baseline 和所有候选。
- `edges`：父节点到子节点的边。
- `summary`：节点数、边数、状态分布、方向分布。
- `reward`：reward 含义和颜色映射说明。

节点字段包含：

- `id`
- `parent_id`
- `status`
- `reward`
- `reward_color`
- `reward_normalized`
- `val_bpb`
- `direction_key`
- `search_mode`
- `operator`
- `description`
- `intent`
- `rationale`
- `novelty_judge`
- `result`

reward 颜色约定：

```text
正 reward: 绿色，越深越好
负 reward: 红色，越深越差
未知 reward: 灰色
```

后续静态或实时 UI 都应读取这个 JSON，而不是直接解析 registry/results/queue 文件。

### 动态调度

不要按总 budget 刚性划分阶段，而是把 budget prior 和触发规则结合起来：

- 默认大部分预算进入 `mechanism_search`。
- 只有训练不稳或新机制需要局部公平比较时进入 `control_tune`。
- 只有出现明显 winner 后才进入 `verify_winner`、`merge_validate` 或 `simplify_winner`。
- 对 near-miss 或重复失败方向使用 `pivot_after_failure`，而不是继续原地调参。

### 方向配额

限制同一 `direction_key` 的并发数量：

```text
active + queued + running <= 1 或 2
```

这样可以避免多个并行 agent 同时扎堆到同一方向。

### 外层 meta-autoresearch

每 30 到 50 次实验后，触发一次 meta-review：

- 总结哪些 operator 有效。
- 总结哪些 direction family 失败率高。
- 建议新增或删除搜索算子。
- 建议调整 reward 权重。

第一版不要让 meta-agent 自动改 runner 代码。它只产出建议文件，由人工确认后再修改。

## 推荐实施顺序

1. 实现 `search_policy.py` 和 `SearchPlan`。
2. 在 `runner.py` 的 `_generation_loop` 中为每个候选生成 `SearchPlan`。
3. 修改 `_create_candidate(...)`，强制 agent 按 `SEARCH_PLAN` 修改代码。
4. 把 `search_plan` 写入 registry 和候选 `meta.json`。
5. 增加 reward 计算，并写回 registry。
6. 基于 reward 和 visit count 改进父节点选择。
7. 增加 `SearchEpoch`、epoch plan 队列和软 barrier。
8. 增加 epoch summary，指导下一层搜索。
9. 增加动态 `search_mode`，区分 `control_tune`、`mechanism_search`、`verify_winner` 等模式。
10. 修改 novelty judge，`mechanism_search` 禁止纯调参候选。
11. 增加方向配额。
12. 最后再考虑 meta-autoresearch。

## 最小可交付版本

最小但有效的版本只需要完成：

- `autorunner/search_policy.py`
- `SearchPlan`
- 四个固定 operator
- 简单 epsilon-greedy / UCB 选择
- runner 强制把 `SEARCH_PLAN` 注入 agent prompt
- registry 记录 `search_operator`、`search_plan_id`、`search_parent_id`

这已经足以让系统从“agent 自由提案”变成“搜索算法指导 agent 沿优化方向前进”。

如果要加入同步机制，最小扩展版本再增加：

- `SearchEpoch`
- 当前 epoch 的 plan 队列
- 软 barrier 结束条件
- `epoch_summary_{epoch_id}.json`
- registry 记录 `epoch_id` 和 `search_depth`

这样系统就会从简单的并行候选生成，升级为分层搜索树上的 epoch 式并行搜索。

如果要避免系统停留在调参层面，最小扩展版本再增加：

- `search_mode` 字段
- `control_tune` / `mechanism_search` / `local_tune_after_mechanism` operator 或 mode 类型
- `mechanism_search` 纯调参禁止规则
- novelty judge 输出 `innovation_type`、`is_pure_tuning`、`mechanism_changed`

这样可以把调参限制在支撑 baseline 和局部公平比较的角色上，把主要实验预算留给机制级创新。
