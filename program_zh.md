# autoresearch

这是一个让 LLM 进行自主研究的实验。

## 初始化设置

要设置一个新实验，请与用户合作完成以下步骤：

1. **商定运行标签**：根据今天的日期提出一个标签（例如 `mar5`）。分支 `autoresearch/<tag>` 必须不存在——这是一个全新的运行。
2. **创建分支**：`git checkout -b autoresearch/<tag>`，基于当前的 master 分支。
3. **阅读范围内的文件**：仓库很小。阅读以下文件以获得完整上下文：
   - `README.md` — 仓库上下文。
   - `prepare.py` — 固定常量、数据准备、分词器、数据加载器、评估。不允许修改。
   - `train.py` — 你需要修改的文件。模型架构、优化器、训练循环。
4. **验证数据存在**：检查 `~/.cache/autoresearch/` 是否包含数据分片和分词器。如果没有，告诉用户运行 `uv run prepare.py`。
5. **初始化 results.tsv**：创建 `results.tsv`，只包含表头行。基线将在第一次运行后记录。
6. **确认并开始**：确认设置看起来没问题。

获得确认后，启动实验。

## 实验

每个实验在单 GPU 上运行。训练脚本运行固定的 **5 分钟时间预算**（壁钟时间，不包括启动/编译时间）。你只需要运行：`uv run train.py`。

**你可以做的：**
- 修改 `train.py` —— 这是你唯一编辑的文件。一切都可以尝试：模型架构、优化器、超参数、训练循环、批次大小、模型大小等。

**你不能做的：**
- 修改 `prepare.py`。它是只读的。它包含固定的评估、数据加载、分词器和训练常量（时间预算、序列长度等）。
- 安装新包或添加依赖。你只能使用 `pyproject.toml` 中已有的内容。
- 修改评估工具。`prepare.py` 中的 `evaluate_bpb` 函数是真实指标。

**目标很简单：获得最低的 val_bpb。** 由于时间预算固定，你不需要担心训练时间——它总是 5 分钟。一切都可以尝试：改变架构、优化器、超参数、批次大小、模型大小。唯一的限制是代码必须能运行不崩溃，并在时间预算内完成。

**VRAM** 是一个软约束。为了获得有意义的 val_bpb 提升，一些增加是可以接受的，但不应该急剧膨胀。

**简洁性标准**：在其他条件相同的情况下，越简单越好。一个小的改进如果带来了丑陋的复杂性，那就不值得。相反，删除一些东西并获得相同或更好的结果是一个很好的结果——这是一个简化上的成功。在评估是否保留某个更改时，要权衡复杂性成本与改进幅度。一个增加 20 行 hack 代码但 val_bpb 仅提升 0.001 的改进？可能不值得。一个通过删除代码使 val_bpb 提升 0.001 的改进？绝对保留。改进几乎为零但代码简单很多的？保留。

**第一次运行**：你的第一次运行应该始终是建立基线，所以你会按原样运行训练脚本。

## 输出格式

脚本完成后会打印如下摘要：

```
---
val_bpb:          0.997900
training_seconds: 300.1
total_seconds:    325.9
peak_vram_mb:     45060.2
mfu_percent:      39.80
total_tokens_M:   499.6
num_steps:        953
num_params_M:     50.3
depth:            8
```

注意，脚本配置为始终在 5 分钟后停止，因此根据计算机的计算平台不同，数字可能会有所不同。你可以从日志文件中提取关键指标：

```
grep "^val_bpb:" run.log
```

## 记录结果

实验完成后，将其记录到 `results.tsv`（制表符分隔，不是逗号分隔——逗号在描述中会出问题）。

TSV 有一行列头和 5 列：

```
commit	val_bpb	memory_gb	status	description
```

1. git 提交哈希（短，7 个字符）
2. 达到的 val_bpb（例如 1.234567）—— 崩溃时使用 0.000000
3. 峰值内存（GB），保留一位小数（例如 12.3 — 将 peak_vram_mb 除以 1024）—— 崩溃时使用 0.0
4. 状态：`keep`、`discard` 或 `crash`
5. 简短文字描述这个实验尝试了什么

示例：

```
commit	val_bpb	memory_gb	status	description
a1b2c3d	0.997900	44.0	keep	baseline
b2c3d4e	0.993200	44.2	keep	increase LR to 0.04
c3d4e5f	1.005000	44.0	discard	switch to GeLU activation
d4e5f6g	0.000000	0.0	crash	double model width (OOM)
```

## 实验循环

实验在专用分支上运行（例如 `autoresearch/mar5` 或 `autoresearch/mar5-gpu0`）。

永远循环：

1. 查看 git 状态：我们当前所在的分支/提交
2. 通过直接修改代码，用实验性想法调整 `train.py`。
3. git 提交
4. 运行实验：`uv run train.py > run.log 2>&1`（重定向所有输出——不要使用 tee 或让输出淹没你的上下文）
5. 读取结果：`grep "^val_bpb:\|^peak_vram_mb:" run.log`
6. 如果 grep 输出为空，说明运行崩溃了。运行 `tail -n 50 run.log` 读取 Python 堆栈跟踪并尝试修复。如果多次尝试后仍无法正常工作，放弃。
7. 将结果记录到 tsv（注意：不要提交 results.tsv 文件，让它保持不被 git 跟踪）
8. 如果 val_bpb 有所改进（更低），你就"推进"分支，保留 git 提交
9. 如果 val_bpb 相同或更差，你就 git reset 回开始的地方

这个想法是，你是一个完全自主的研究员在尝试各种想法。如果它们有效，就保留。如果无效，就丢弃。你在推进分支以便迭代。如果你觉得自己在某个方面陷入了困境，可以回滚，但你应该非常非常谨慎地这样做（如果有的话）。

**超时**：每个实验应该总共花费约 5 分钟（加上启动和评估开销的几秒钟）。如果运行超过 10 分钟，杀掉它并将其视为失败（丢弃并还原）。

**崩溃**：如果运行崩溃了（OOM、bug 或其他），运用你的判断：如果是容易修复的愚蠢问题（例如拼写错误、缺少导入），修复它并重新运行。如果这个想法本身从根本上就是坏的，就跳过它，在 tsv 中记录 "crash" 作为状态，然后继续。

**永不停止**：一旦实验循环开始（在初始设置之后），不要暂停询问人类是否应该继续。不要问"我应该继续吗？"或"这是一个好的停止点吗？"。人类可能在睡觉，或者不在电脑前，希望你无限期地继续工作，直到你被手动打断。你是自主的。如果你用完了想法，再努力思考——阅读代码中引用的论文，重新阅读范围内的文件以寻找新的角度，尝试结合之前的接近成功，尝试更激进的架构改变。循环一直运行，直到人类打断你为止。

作为一个用例示例，用户可能会在你运行时离开去睡觉。如果每个实验花费你约 5 分钟，那么你可以每小时运行约 12 个，在普通人类睡眠期间总共约 100 个。用户醒来时就会看到实验结果，全部是你在他们睡觉时完成的！
